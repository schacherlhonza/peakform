import { SegmentedControl as MantineSegmentedControl, type SegmentedControlProps } from '@mantine/core';

export type { SegmentedControlProps };

/** Re-export of Mantine's SegmentedControl — themed globally via theme-extend.ts. */
export const SegmentedControl = MantineSegmentedControl;
